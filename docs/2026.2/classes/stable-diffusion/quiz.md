<div id="quiz-stable-diffusion"></div>
<script>
buildQuiz('stable-diffusion', 'Stable Diffusion & Diffusion Models', [
  {
    q: 'What happens in the forward diffusion process?',
    opts: [
      'The model progressively generates an image from noise',
      'Gaussian noise is progressively added to a real image until it becomes pure noise',
      'The image is compressed by the VAE encoder into latent space',
      'The U-Net predicts attention weights for each timestep'
    ],
    ans: 1,
    exp: 'Forward (noising): q(x_t|x_{t-1}) = N(√α_t·x_{t-1}, β_t·I). In T steps, x_T ≈ N(0,I). This process is fixed, not trained. The model learns the reverse process (denoising). In SD, this occurs in the VAE latent space.'
  },
  {
    q: 'What does the U-Net predict in DDPM (Denoising Diffusion Probabilistic Models)?',
    opts: [
      'The clean original image x_0 directly',
      'The noise ε that was added at each step, which is then subtracted',
      'Attention weights between patches of the noisy image',
      'The probability of each pixel belonging to the original image'
    ],
    ans: 1,
    exp: 'ε-prediction: ε_θ(x_t, t) ≈ ε. The loss is ||ε - ε_θ(x_t, t)||². Knowing the noise allows estimating x_0 and computing x_{t-1}. Alternative: v-prediction (SD 2.x and 3) which is more stable at extreme timesteps.'
  },
  {
    q: 'What is the latent space in Stable Diffusion (Latent Diffusion Model)?',
    opts: [
      'The vocabulary space of the CLIP text encoder',
      'A compressed representation of images generated by the VAE, where diffusion occurs instead of pixel space',
      'The parameter space of the U-Net during training',
      'The Gaussian noise distribution at timestep T'
    ],
    ans: 1,
    exp: 'LDM (Rombach et al., 2022): diffusing directly on 512×512×3 pixels is expensive. The VAE compresses to 64×64×4 (8× factor). Diffusion runs in latent space — much more efficient with comparable quality.'
  },
  {
    q: 'What is Classifier-Free Guidance (CFG)?',
    opts: [
      'Using a separate classifier to guide diffusion toward the desired class',
      'Training the model with and without conditioning and interpolating predictions to amplify prompt alignment',
      'A regularization technique that randomly removes conditioning',
      'A fine-tuning method that requires no class labels'
    ],
    ans: 1,
    exp: 'CFG: ε_guided = ε_uncond + scale × (ε_cond - ε_uncond). With scale=7.5, the model is strongly pushed in the prompt direction. During training, 50% of the time conditioning is zeroed to teach unconditional prediction.'
  },
  {
    q: 'Which architectural component allows the U-Net to integrate textual prompt information?',
    opts: [
      'Batch Normalization in the residual blocks',
      'Cross-Attention between U-Net features and text embeddings',
      'Concatenating the text embedding with the noise input',
      'A final softmax layer that selects vocabulary tokens'
    ],
    ans: 1,
    exp: 'In the U-Net residual blocks: Q comes from noisy image features, K and V come from the text encoder embeddings (CLIP/T5). Cross-attention injects textual conditioning at each resolution level of the U-Net.'
  }
]);
</script>
