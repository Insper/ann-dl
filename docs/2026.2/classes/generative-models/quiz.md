<div id="quiz-gen-models"></div>
<script>
buildQuiz('gen-models', 'Generative Models', [
  {
    q: 'What does a generative model learn, unlike a discriminative model?',
    opts: [
      'Only how to classify samples into categories',
      'The joint distribution p(x, y) or marginal p(x), enabling generation of new samples',
      'Only the decision boundary between classes without modeling the data',
      'Only regression between continuous variables'
    ],
    ans: 1,
    exp: 'Discriminative models learn p(y|x) — the class probability given input. Generative models learn p(x) or p(x,y) — the data distribution itself, allowing sampling of new plausible instances.'
  },
  {
    q: 'What is the latent space?',
    opts: [
      'The hyperparameter space of the model',
      'A compressed continuous representation where nearby points correspond to similar data',
      'The token vocabulary set of an LLM',
      'The output layer of the model that produces probabilities'
    ],
    ans: 1,
    exp: 'The latent space is a reduced dimensionality where each point z represents data structure. In VAEs, z ~ N(0,I) and the decoder G(z) generates realistic data. Interpolating between two latent points produces smooth transitions.'
  },
  {
    q: 'What are the three main families of deep generative models?',
    opts: [
      'CNN, RNN, Transformer',
      'GANs (adversarial), VAEs (variational), Diffusion Models',
      'Supervised, Unsupervised, Reinforcement Learning',
      'Autoencoder, Autoregressive, Attention-based'
    ],
    ans: 1,
    exp: 'GANs: generation via adversarial game G vs D. VAEs: generation via Gaussian latent space and ELBO. Diffusion: generation via noise reversal. Each family has trade-offs in quality, diversity, and training stability.'
  },
  {
    q: 'What is the "curse of dimensionality" in generative models?',
    opts: [
      'The impossibility of training models with more than 1 billion parameters',
      'In high-dimensional spaces, data becomes sparse and Euclidean distances lose meaning',
      'The fact that more dimensions inevitably cause overfitting',
      'The limitation of GPUs in processing high-dimensional tensors'
    ],
    ans: 1,
    exp: 'In R^d with large d, volume grows exponentially and data becomes extremely sparse. This makes density estimation very difficult. That is why models like VAE and diffusion work in compressed latent spaces.'
  },
  {
    q: 'Why does self-supervised generation allow training with unlabeled data?',
    opts: [
      'Because generative models do not need input examples',
      'Because the data itself is the target — the model learns to reconstruct or predict parts of the data without external labels',
      'Because labels are automatically generated by an LLM',
      'Because training uses only reinforcement learning without labels'
    ],
    ans: 1,
    exp: 'In GPT (next-token prediction) and MAE (masked autoencoding), targets are extracted from the data itself. No manual annotation needed — any text or image is a training example. This enables internet-scale training.'
  }
]);
</script>
